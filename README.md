```bash
$ pkg update && upgrade

$ termux-setup-storage

$ pkg install python 

$ pkg install git 

$ pip install bs4 

$ pip install requests 

$ pip install mechanize

$ pip install futures 

$ pip install colorama 

$ git clone https://github.com/Fanoragans/G2 

$ cd G2 

$ python ahad-vau-enc.py
```
